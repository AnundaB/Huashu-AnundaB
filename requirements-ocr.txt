# huashu-md-html - OCR Python dependencies
#
# OCR is heavier than the default install. Install this add-on only when you
# need OCR for scanned PDFs, screenshots, or image-heavy documents.
#
# PaddleOCR may download OCR models on first use, depending on engine behavior.

paddleocr
paddlepaddle
pymupdf
