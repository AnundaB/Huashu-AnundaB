# huashu-md-html - default lightweight Python dependencies
#
# This is the recommended default install for fast, common extraction.
# Heavy OCR dependencies are optional; install requirements-ocr.txt only
# when you need OCR for scanned PDFs or images.

-r requirements-core.txt
